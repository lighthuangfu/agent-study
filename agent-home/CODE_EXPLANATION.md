# 代码架构与流程说明

## 📋 项目概览

这是一个基于 **LangGraph** 构建的智能 Agent 系统，主要功能是：
- 理解用户意图
- 获取天气信息
- 抓取 RSS 新闻源
- 生成综合早报

---

## 🏗️ 整体架构

```
┌─────────────┐
│  backend.py │  ← FastAPI 服务器入口
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  graph.py   │  ← LangGraph 工作流定义
└──────┬──────┘
       │
       ├───► nodes.py      (4个节点函数)
       ├───► states.py     (状态定义)
       ├───► model.py      (LLM 初始化)
       └───► tools.py       (8个工具函数)
```

---

## 🔄 数据流与执行流程

### 1. 请求入口 (`backend.py`)

```python
POST /run-task
  ↓
run_agent_task() 接收请求
  ↓
构造初始状态 inputs
  ↓
调用 graph.astream(inputs) 开始执行
  ↓
通过 SSE (Server-Sent Events) 实时推送结果
```

### 2. 工作流执行 (`graph.py`)

```
START
  ↓
intent_expert (意图理解节点)
  ↓
    ├─► [ROUTE=weather] ──► weather_expert ──┐
    ├─► [ROUTE=rss]     ──► rss_expert     ──┤
    └─► [ROUTE=none]    ──► (直接跳过)     ──┤
                                              ↓
                                        aggregator (汇总节点)
                                              ↓
                                            END
```

### 3. 状态流转 (`states.py`)

状态是一个 `TypedDict`，包含：
- `messages`: 消息列表（用于 LLM 对话）
- `rss_summaries`: RSS 抓取结果列表
- `weather_report`: 天气报告字符串
- `user_input`: 用户原始输入
- `user_intent`: 解析后的用户意图
- `intent_route`: 路由决策（weather/rss/none）

每个节点返回一个字典，LangGraph 会自动合并到状态中。

---

## 📦 核心模块详解

### 1. `states.py` - 状态定义

```python
class MergeAgentState(TypedDict):
    messages: Annotated[List[BaseMessage], operator.add]  # 使用 operator.add 合并消息
    rss_summaries: List[str]
    weather_report: str
    user_input: str
    user_intent: str
    intent_route: str
```

**关键点：**
- `messages` 使用 `operator.add` 作为 reducer，使得并行分支的消息可以自动合并
- 其他字段是普通字段，后写入的值会覆盖先写入的值

---

### 2. `model.py` - LLM 初始化

```python
_llm = ChatOpenAI(
    model=model,        # 从环境变量读取
    base_url=base_url,  # API 地址
    api_key=api_key,    # API 密钥
    temperature=0.1,    # 低温度，输出更稳定
)

model_with_tools = _llm.bind_tools(ALL_TOOLS)  # 绑定所有工具
```

**作用：**
- 初始化大语言模型（支持自定义 API，如豆包）
- 将工具绑定到模型，让模型可以调用工具

---

### 3. `tools.py` - 工具集合

定义了 8 个工具，Agent 可以调用这些工具完成任务：

#### 3.1 网络工具
- **`search`**: DuckDuckGo 搜索（无需 API Key）
- **`web_fetch`**: 直接抓取网页原始内容（前5000字符）
- **`web_browser`**: 使用 WebBaseLoader 智能解析网页正文

#### 3.2 RSS 工具
- **`rss_reader`**: 读取 RSS 源，返回最新 5 篇文章
  - 自动处理编码问题
  - 模拟浏览器请求头
  - 处理 SSL 证书问题

#### 3.3 定位与天气
- **`get_current_location`**: 通过 IP 获取城市位置
- **`get_weather`**: 天气查询（实际是引导 Agent 使用 search 工具）

#### 3.4 向量数据库工具
- **`save_vectors_to_qdrant`**: 将向量写入 Qdrant
  - 自动创建集合（如果不存在）
  - 支持自定义 ID 和 payload（元数据）
  
- **`index_web_page_to_qdrant`**: 完整的网页索引流程
  ```
  网页 URL
    ↓
  web_browser() 抓取内容
    ↓
  _split_text_into_chunks() 切分文本
    ↓
  _simple_hash_embedding() 向量化（简单哈希实现）
    ↓
  save_vectors_to_qdrant() 写入数据库
  ```

**辅助函数：**
- `_simple_hash_embedding()`: 简单的文本向量化（用于 demo，可替换为真实 embedding 模型）
- `_split_text_into_chunks()`: 按句子切分长文本

---

### 4. `nodes.py` - 节点实现

#### 4.1 `intent_agent_node` - 意图理解节点

**功能：**
1. 解析用户输入，理解意图
2. 决定路由：weather / rss / none

**实现逻辑：**
```python
1. 检查是否有用户输入
   - 无输入 → 返回默认意图，路由为 "none"
   
2. 调用 LLM 分析意图
   - 要求 LLM 输出意图总结 + ROUTE=xxx
   
3. 解析 LLM 输出
   - 提取最后一行 "ROUTE=xxx"
   - 剩余部分作为意图文本
   
4. 关键字纠错（双重保险）
   - 如果包含天气关键词 → 强制路由到 weather
   - 如果包含新闻关键词 → 强制路由到 rss
```

**返回：**
```python
{
    "user_intent": "用户想看今天的新闻",
    "intent_route": "rss"
}
```

---

#### 4.2 `weather_agent_node` - 天气专家节点

**功能：** 获取天气信息

**实现特点：**
- 使用**子 Agent** 模式：创建一个新的 Agent executor，让它调用工具
- **超时控制**：使用线程池 + 120秒超时，防止卡死
- **并发安全**：在线程中创建新的 executor（避免线程安全问题）

**流程：**
```python
1. 创建子 Agent（带工具）
2. 构造天气查询 prompt
3. 在子线程中执行（避免阻塞）
4. 等待结果（最多120秒）
5. 返回天气报告
```

**返回：**
```python
{
    "weather_report": "北京今天晴天，温度 15-25°C..."
}
```

---

#### 4.3 `rss_agent_node` - RSS 专家节点

**功能：** 并发抓取多个 RSS 源

**实现特点：**
- **多线程并发**：使用 `ThreadPoolExecutor(max_workers=5)` 同时处理 5 个 RSS 源
- **每个线程独立 Agent**：每个 URL 在线程内部创建独立的 Agent executor
- **默认 RSS 源列表**：
  - 少数派
  - 阮一峰博客
  - 微博热搜
  - 新华网
  - 华尔街日报中文版

**流程：**
```python
1. 定义 5 个 RSS URL
2. 创建线程池（5个线程）
3. 每个线程：
   - 创建独立的 Agent executor
   - 调用 rss_reader 工具
   - 格式化输出为 Markdown
4. 收集所有结果
5. 返回汇总列表
```

**返回：**
```python
{
    "rss_summaries": [
        "1. [文章标题](链接)\n   - 摘要...",
        "2. [文章标题](链接)\n   - 摘要...",
        ...
    ]
}
```

---

#### 4.4 `aggregator_node` - 汇总节点

**功能：** 汇总所有信息，生成最终报告

**实现：**
```python
1. 从状态中提取数据：
   - weather_report
   - rss_summaries
   - user_intent

2. 组装 Markdown 格式报告：
   - 天气情况
   - RSS 订阅（按来源分组）
   - 用户意图

3. 返回 AIMessage（关键！）
   - 必须返回 messages 字段
   - 这样 graph.invoke() 的结果中才能获取 content
```

**返回：**
```python
{
    "messages": [AIMessage(content="完整的 Markdown 报告...")]
}
```

---

### 5. `graph.py` - 工作流定义

**核心概念：**
- **StateGraph**: 状态图，管理节点间的数据流转
- **节点 (Node)**: 执行具体任务的函数
- **边 (Edge)**: 定义节点间的连接关系
- **条件边 (Conditional Edge)**: 根据状态动态决定下一步

**工作流结构：**
```python
1. 创建 StateGraph
   workflow = StateGraph(MergeAgentState)

2. 添加节点
   workflow.add_node("intent_expert", intent_agent_node)
   workflow.add_node("weather_expert", weather_agent_node)
   workflow.add_node("rss_expert", rss_agent_node)
   workflow.add_node("aggregator", aggregator_node)

3. 添加边
   - START → intent_expert (固定边)
   - intent_expert → ? (条件边，根据 intent_route 决定)
   - weather_expert → aggregator (固定边)
   - rss_expert → aggregator (固定边)
   - aggregator → END (固定边)

4. 编译
   graph = workflow.compile()
```

**条件路由函数：**
```python
def route_from_intent(state: MergeAgentState) -> str:
    route = state.get("intent_route", "none")
    return route if route in ("weather", "rss", "none") else "none"
```

---

### 6. `backend.py` - FastAPI 服务器

**主要功能：**

#### 6.1 流式响应 (`event_generator`)
```python
async def event_generator(inputs):
    async for event in graph.astream(inputs):
        # 每个节点执行完，都会触发一次 event
        for node_name, state in event.items():
            # 发送进度日志
            yield f"data: {日志JSON}\n\n"
    
    # 最后发送最终结果
    yield f"data: {结果JSON}\n\n"
```

**SSE 格式：**
- 前端通过 EventSource 接收
- 每个事件格式：`data: {JSON}\n\n`
- 事件类型：
  - `intent`: 意图理解结果
  - `log`: 进度日志
  - `result`: 最终结果
  - `error`: 错误信息

#### 6.2 API 端点
- `GET /`: 健康检查
- `POST /run-task`: 执行 Agent 任务

**请求格式：**
```json
{
    "user_id": "default_user",
    "user_input": "我想看今天的新闻"
}
```

---

## 🔑 关键设计模式

### 1. **子 Agent 模式**
在节点内部创建新的 Agent executor，让 Agent 自主调用工具完成任务。

**优点：**
- 灵活性高，Agent 可以自主决策使用哪些工具
- 适合复杂任务

**示例：**
```python
weather_executor = create_agent(_llm, ALL_TOOLS)
result = weather_executor.invoke({"messages": [HumanMessage(content=prompt)]})
```

---

### 2. **并发处理模式**
使用 `ThreadPoolExecutor` 并发执行多个任务。

**关键点：**
- 每个线程创建独立的 Agent executor（避免线程安全问题）
- 使用超时控制，防止单个任务卡死整个系统

**示例：**
```python
with ThreadPoolExecutor(max_workers=5) as executor:
    futures = [executor.submit(process_url, url) for url in urls]
    results = [f.result() for f in futures]
```

---

### 3. **状态合并模式**
使用 `operator.add` 作为 reducer，自动合并并行分支的消息。

**示例：**
```python
messages: Annotated[List[BaseMessage], operator.add]
# 如果两个节点都返回 messages，会自动合并成一个列表
```

---

### 4. **流式响应模式**
使用 SSE (Server-Sent Events) 实时推送执行进度。

**优点：**
- 用户体验好，可以看到实时进度
- 不需要前端轮询

---

## 🐛 注意事项

### 1. 工具中的 `models` 未导入
在 `tools.py` 第 185 行使用了 `models.VectorParams`，但未导入：
```python
from qdrant_client import QdrantClient, models  # 需要添加 models
```

### 2. `get_weather` 函数有语法错误
第 142 行 `print` 语句不完整，应该删除或补全。

### 3. 线程安全
- Agent executor 不是线程安全的
- 每个线程必须创建独立的 executor
- 代码中已经正确处理了这一点

---

## 📝 总结

这是一个典型的 **多 Agent 协作系统**：
1. **意图理解 Agent** 负责路由决策
2. **天气 Agent** 和 **RSS Agent** 并行执行具体任务
3. **汇总 Agent** 整合所有结果

整个系统通过 LangGraph 的状态管理机制，实现了：
- 灵活的路由决策
- 并发的任务执行
- 自动的状态合并
- 实时的进度推送
